// Conditions par défaut du plan de règlement (= comportement historique figé,
// appliqué quand aucune condition n'est saisie dans Paramètres → Modes de règlement).
export const DEFAULT_NB_CHEQUE = 4
export const DEFAULT_DELAIS = { VIREMENT: [90], LCR: [60, 90], GARANT: [120], GMS: [120], PRELEVEMENT: [30] }
